# This config file is used by ros2_control
controller_manager:
  ros__parameters:
    update_rate: 100  # Hz

    x500_group_controller:
      type: joint_trajectory_controller/JointTrajectoryController


    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

x500_group_controller:
  ros__parameters:
    joints:
      - virtual_joint
    command_interfaces:
      []
    state_interfaces:
      []